import * as React from 'react';
import './App.css';

export default class Home extends React.Component<{}, {}> {

    render() {
        return (
            <div>
                React + Jest + React Router + MobX + TypeScript
            </div>
        );
    }

}
